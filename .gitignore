# Vídeos exportados (se suben a YouTube, no al repo)
/*.mp4
/*.mkv
/*.webm

# Vídeos SOLO EN LOCAL (Opción C): no se suben a GitHub — la app los usa desde videos/.
# (Los vídeos ya subidos antes siguen en el repo; esto solo evita subir los nuevos.)
/videos/

# Imágenes de prueba
/preview-*.png
/prueba-*.png
/selftest*.png

# App de escritorio (Electron)
/node_modules/
/package-lock.json
/dist/
/out/

# transcripción (binarios y modelos, no subir)
/tools/
